import 'package:flutter/material.dart';

class TodayStudyReviewMainFrame extends StatelessWidget {
  const TodayStudyReviewMainFrame({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('TodayStudyReviewMainFrame'));
  }
}
